/**
 * ConnectionLines — formerly complex SVG bezier curves with data packets.
 * Replaced by simple CSS dividers integrated directly in DashboardMap.
 * This file is kept as a no-op to avoid broken imports elsewhere.
 */
export function ConnectionLines() {
  return null;
}
